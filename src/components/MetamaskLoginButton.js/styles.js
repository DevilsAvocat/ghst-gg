import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    button: {
        whiteSpace: 'nowrap',
        textTransform: 'none'
    },
    address: {
        color: theme.palette.primary.main
    }
}));
